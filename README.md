# maali.garden

The Maali website, served by GitHub Pages at https://maali.garden.

One page, no build step, no external requests. Fonts are self-hosted in `fonts/`.
The update channel lives separately at updates.getmaali.com (the `maali-releases` repo)
and never depends on this site.
